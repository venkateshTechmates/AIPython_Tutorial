import type { PlaygroundConfig } from "../../types";
export const pg11: PlaygroundConfig = { partId: "11", endpoints: [{ id: "health", method: "GET", path: "/health", description: "Health check for Part 11", exampleBody: null }] };
